#include "main.h"
* main - Entry point
*  *
*   * Return: nothing, return void
*    */

int main(void)
{
	for (i = 0; i < 9; i++)
			{
						_putchar(holbText[i]);
							}
		_putchar('\n');
			return (0);
			return (0);
}

